[build]
builder = "RAILPACK"
buildCommand = "pip install ."

[deploy]
preDeployCommand = ["alembic upgrade head"]
startCommand = "korgan-telegram"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
