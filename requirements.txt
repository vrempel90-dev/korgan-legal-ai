aiogram>=3.13,<4
openai>=1.100
# Основной провайдер модели (korgan/anthropic_responses.py). Без объявленной
# зависимости build_legal_client поймал бы ImportError и молча ушёл на OpenAI:
# прод работал бы, но основным провайдером Anthropic так и не стал бы.
anthropic>=1.2,<2
pydantic-settings>=2.5
python-dotenv>=1.0
python-docx>=1.1
pypdf==6.16.1
# Full-page rendering is required because Kaspi PDF receipts can draw the fiscal
# QR directly into page content instead of exposing it through page.images.
pypdfium2>=5.0,<6
numpy>=2.0,<3
# OpenCV 4.14 regresses QR decoding for long fiscal URLs in rendered PDFs.
# Keep the verified 4.10-4.13 line until the regression is fixed upstream.
opencv-python-headless>=4.10,<4.14
asyncpg>=0.30,<1
cryptography>=45,<47
fastapi>=0.116,<1
uvicorn[standard]>=0.35,<1
python-multipart>=0.0.20,<1
# Клиент Telegram Bot API в korgan/miniapp_telegram_delivery.py.
# Раньше приезжал транзитивно с openai; openai 3.x перешёл на httpx2,
# поэтому зависимость объявлена явно — без неё не импортируется всё
# приложение korgan.miniapp_api_recovery_cors.
httpx>=0.27,<1
pytest>=8.3
